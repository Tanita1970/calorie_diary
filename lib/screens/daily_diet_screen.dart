import 'package:flutter/material.dart';

class DailyDietScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Рацион дня'),
    );
  }
}